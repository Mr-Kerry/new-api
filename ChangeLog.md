# Changelog

## 本次更新

### 新增邀请返利功能

- 新增邀请返利机制
- 支持用户通过邀请链接邀请新用户注册
- 支持被邀请用户充值后给邀请人发放返利奖励
- 优化邀请关系与充值奖励流程

### 优化浏览器标题显示

- 修复自定义系统名称后，浏览器标签页仍显示 `New API` 的问题
- 前端加载系统配置后，根据 `/api/status` 返回的 `system_name` 动态更新浏览器标签页标题
- 更新入口 HTML 默认标题，减少首次加载时显示旧标题的问题
